# concerns.md — GENERATED-STUB (no warroom concerns file; expand with project-specific risks)

[C] Demo failure: the condition→confidential-verdict→FXRP-release flow must run end-to-end without manual intervention in the recorded demo
[C] FCC reality: the FCC extension must produce a signed verdict a Solidity contract verifies; Coston2 = SIMULATED TEE (MODE=0) — must be labeled honestly, not hidden
[C] FDC latency: Web2Json round (~180s) must be pre-warmed so the 90s demo shows verdict→release live, not a fake
[C] Asset-track bar: the FXRP action must be a real lifecycle touchpoint with a receipt, NOT a bare ERC-20 transfer of pre-minted FXRP
[I] Confidential half load-bearing: the private policy must be genuine private terms; if it reads as removable, the CCA-track entry is weak
[I] Scope: ~26h solo, EMERGENCY mode — demo-first; everything not in the 90s demo path is a labeled stub
[A] Songbird real-TEE: deploying to Songbird for a real hardware TEE is advisory (verify availability); Coston2 sim-TEE is acceptable if labeled
